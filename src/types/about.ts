export interface Employee {
  id: string;
  name: string;
  email: string;
  role: string;
  bio: string;
  profile_image: string;
  created: string;
  phone: string;
}

export interface Timeline {
  id: string;
  title: string;
  date: string;
  body: string;
  created: string;
}
